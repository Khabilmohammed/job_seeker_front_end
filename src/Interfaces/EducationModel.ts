export interface EducationModel {
  educationId?: string;
  degree: string;
  institution: string;
  fieldOfStudy: string; // Added field for the field of study
  startDate: string;    // Added field for the start date
  endDate: string;      // Added field for the end date
  }
    